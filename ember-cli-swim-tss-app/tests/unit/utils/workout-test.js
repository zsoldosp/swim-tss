import workout from 'ember-cli-swim-tss-app/utils/workout';
import { module, test } from 'qunit';

module('Unit | Utility | workout', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = workout();
    assert.ok(result);
  });
});
